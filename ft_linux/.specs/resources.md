# Resources

Reference material for the project (chapter III.0.1 Resources).

- **The Bible** — in the LFS context this is the *Linux From Scratch* book.
  *(Anchor text in the source: "The Bible"; the target URL is embedded in the
  PDF and not displayed inline.)*
- **How to build a Linux kernel** — there are plenty of online resources that
  describe step-by-step recipes to configure, build, and run a custom Linux
  kernel.
- **Autotools** — build-system tooling. *(Anchor text in the source:
  "Autotools"; the target URL is embedded in the PDF and not displayed
  inline.)*

## FHS / filesystem standards

The subject references the filesystem hierarchy standards in two places, both
as embedded hyperlinks whose target URLs are not shown as plain text:

- The **Goals** chapter links the anchor text "standards" when requiring an
  FHS-compliant hierarchy (typically the FHS / Filesystem Hierarchy Standard).
  See [goals](goals.md).
- The **Instructions** chapter links the anchor texts "this" and "that",
  recommending you read those standards. See
  [general instructions](general-instructions.md).

> Note: For every link above, the actual `href` is an embedded link in the PDF
> rather than visible plain text, so the literal target URLs cannot be
> transcribed from the source.

---

Related: [general instructions](general-instructions.md), [goals](goals.md).
